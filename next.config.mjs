/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['via.placeholder.com', 'another-example.com', 'images.pexels.com'],
    },
};

export default nextConfig;
